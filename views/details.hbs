<section class="expense-report">

    <h1>ID: {{expense.id}}</h1>
    <div class="report-date">
        <img src="/static/images/date.png" alt="">
        <p>{{expense.createdDate}}</p>
    </div>

    <div class="report-category">
        <img src="/static/images/amount.png" alt="">
        <p>Equipment - лв {{expense.total}}</p>
    </div>

    <div class="report-merchant">
        <p>{{expense.merchant}}</p>
    </div>

    <div class="report-description">
        <img src="/static/images/hastag.png" alt="">
        <p>{{expense.description}}</p>
    </div>

    <div class="report-action">
        <a disabled href="/expense/delete/{{expense.id}}">Stop tracking</a>
    </div>
</section>